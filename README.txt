
=====================

Notes:
Script is written in python and tested on python 2.7.3 & 2.6 . & php
Tested on cacti 0.8.8a
Tested on ASR9010 & PPPoE


Install:

- copy resouce(directory -including sciprt_querires& snmp_queries& script_server) into CACTI_HOME/resource (directory) 
- copy scripts(directory) into CACTI_HOME/scripts/
- through cacti web interface import templates: cacti_host_template_asr9k-bras-pppoe.xml


